fizzBuzz = function(n)
    for i in range(1, n + 1, 1)
        s = "Fizz" * (i % 3 == 0) + "Buzz" * (i % 5 == 0)
        if s == "" then
            println(i)
        else
            println(s)
        end if
    end for
end function

n = parse_num(read())
fizzBuzz(n)
